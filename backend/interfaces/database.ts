interface IConfig {
	searchTerm: string;
	searchFrequency: Time;
	selectedVendors: string;
	productCategoryId: number;
}

interface IVendor {
	searchTerm: string;
	searchFrequency: Time;
	selectedVendors: string;
	productCategoryId: number;
}
